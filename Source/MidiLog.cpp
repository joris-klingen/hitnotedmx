// Definitions for MidiLog. (Header-only ring storage; this TU exists
// so CMake has a translation unit per source file declared in
// CMakeLists.txt, and to give the audio-thread-safety assertions a
// place to live if we ever add any.)

#include "MidiLog.h"
